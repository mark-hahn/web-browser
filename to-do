
atom-specific omnibox features
  context-help

live reload

recently viewed pages
  
settings
  close web toolbar on last webpage close (false)
  new tabs in front (true)
  
start atom with the command atom http://apple.com
  (opener not getting called)

> could webview give an event with the URL when the mouse hovers over a link?
  You can make use of the preload attribute to load a script in webview to capture 
  the hovering events on links and then use ipc.sendToHost API to tell the host page.

bugs ...
  
